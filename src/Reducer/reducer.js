const initialState = {
  user: {
    _id: null,
    userId: null,
  },
  isLoggedIn: false,
  layout: 'list', // options: list, card, media
  diaries: [],
};

const reducer = () => {};

export default reducer;
